version: 1.0
runtime: python311
build:
  commands:
    build:
      - cd backend
      - python3 -m pip install --upgrade pip
      - python3 -m pip install -r requirements.txt
run:
  command: cd backend && python3 -m uvicorn app.main:app --host 0.0.0.0 --port 8080
  network:
    port: 8080
  env:
    - name: DATABASE_URL
      value: "postgresql://rootcall_admin:Tifum1022##@rootcall-postgres.c0dacq028m25.us-east-1.rds.amazonaws.com:5432/rootcall?sslmode=require"
    - name: FRONTEND_ORIGIN
      value: "https://www.getrootcall.com"
